cmake_minimum_required(VERSION 3.14)
#project(phtree VERSION ${PHTREE_VERSION} LANGUAGES CXX C)

#add_library(phtree STATIC "")
add_subdirectory(common)
add_subdirectory(v16)

#target_include_directories(phtree INTERFACE ${CMAKE_SOURCE_DIR}/include/..)

#set_target_properties(phtree PROPERTIES LINKER_LANGUAGE CXX)

